# ============================================================
# preprocess/defaults.py —— 预处理公共常量
# ------------------------------------------------------------
# 集中维护相机内参等默认值，避免在多个脚本中重复定义导致不一致。
# ============================================================
import numpy as np

# 640x480 占位内参（fx=fy=528，主点在图像中心）。
# 注意：SUN RGB-D 官方数据集的内参种类有限（约 3 组，随传感器不同），
# 真实数据必须从 SUNRGBDtoolbox 元数据（SUNRGBDMeta）读取并替换本值；
# 本常量仅用于缺省回退与冒烟测试。
DEFAULT_K = np.array([[528.0, 0.0, 319.5],
                      [0.0, 528.0, 239.5],
                      [0.0, 0.0, 1.0]], dtype=np.float64)
